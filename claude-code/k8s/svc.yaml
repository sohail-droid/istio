apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: dating
spec:
  selector:
    app: frontend
#    version: v2    #this is the problem
  ports:
    - name: http-front-v2
      port: 80  #svc listening on 
      # targetPort: 80   #container running on
