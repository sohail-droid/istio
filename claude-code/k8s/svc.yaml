apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: dating
spec:
  selector:
    app: frontend
    version: v1    
  ports:
    - port: 80  #svc listening on 
      targetPort: http-front-v1   # 80 or "http-front-v2" if you prefer named port
