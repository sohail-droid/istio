apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: dating
spec:
  selector:
    app: frontend
#    version: v2    #this is the problem
  ports:
    - name: http-frontend
      port: 80  #svc listening on 
      targetPort: 8000   #container running on
